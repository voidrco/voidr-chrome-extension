# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The side panel build output IS shipped with the extension (manifest loads
# sidepanel/dist/index.html directly) — keep it tracked.
!sidepanel/dist/
!sidepanel/dist/**

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# Local env overrides (dev only — production values live in config/env.js)
config/env.local.js

# perfil persistente do E2E (guarda o login)
.e2e-profile/
