diff --git a/node_modules/rrweb/dist/rrweb.cjs b/node_modules/rrweb/dist/rrweb.cjs
index cfb03ef..82b66b0 100644
--- a/node_modules/rrweb/dist/rrweb.cjs
+++ b/node_modules/rrweb/dist/rrweb.cjs
@@ -509,6 +509,13 @@ function splitCssText(cssText, style, _testNoPxNorm = false) {
   const splits = [];
   let iterCount = 0;
   if (childNodes2.length > 1 && cssText && typeof cssText === "string") {
+    // voidr patch — perf: splitCssText's outer scans are still O(n^2) in CSS
+    // length in 2.1.0 (upstream's iterCount guard only bounds the inner k-scan);
+    // skip splitting for large <style> blocks (cosmetic only, replay unaffected).
+    if (cssText.length > 20000) {
+      splits.push(cssText);
+      return splits;
+    }
     let cssTextNorm = normalizeCssString(cssText, _testNoPxNorm);
     const normFactor = cssTextNorm.length / cssText.length;
     for (let i2 = 1; i2 < childNodes2.length; i2++) {
diff --git a/node_modules/rrweb/dist/rrweb.js b/node_modules/rrweb/dist/rrweb.js
index eb6187b..dec512c 100644
--- a/node_modules/rrweb/dist/rrweb.js
+++ b/node_modules/rrweb/dist/rrweb.js
@@ -507,6 +507,13 @@ function splitCssText(cssText, style, _testNoPxNorm = false) {
   const splits = [];
   let iterCount = 0;
   if (childNodes2.length > 1 && cssText && typeof cssText === "string") {
+    // voidr patch — perf: splitCssText's outer scans are still O(n^2) in CSS
+    // length in 2.1.0 (upstream's iterCount guard only bounds the inner k-scan);
+    // skip splitting for large <style> blocks (cosmetic only, replay unaffected).
+    if (cssText.length > 20000) {
+      splits.push(cssText);
+      return splits;
+    }
     let cssTextNorm = normalizeCssString(cssText, _testNoPxNorm);
     const normFactor = cssTextNorm.length / cssText.length;
     for (let i2 = 1; i2 < childNodes2.length; i2++) {
diff --git a/node_modules/rrweb/dist/rrweb.umd.cjs b/node_modules/rrweb/dist/rrweb.umd.cjs
index dea5861..02d7969 100644
--- a/node_modules/rrweb/dist/rrweb.umd.cjs
+++ b/node_modules/rrweb/dist/rrweb.umd.cjs
@@ -555,6 +555,13 @@ function splitCssText(cssText, style, _testNoPxNorm = false) {
   const splits = [];
   let iterCount = 0;
   if (childNodes2.length > 1 && cssText && typeof cssText === "string") {
+    // voidr patch — perf: splitCssText's outer scans are still O(n^2) in CSS
+    // length in 2.1.0 (upstream's iterCount guard only bounds the inner k-scan);
+    // skip splitting for large <style> blocks (cosmetic only, replay unaffected).
+    if (cssText.length > 20000) {
+      splits.push(cssText);
+      return splits;
+    }
     let cssTextNorm = normalizeCssString(cssText, _testNoPxNorm);
     const normFactor = cssTextNorm.length / cssText.length;
     for (let i2 = 1; i2 < childNodes2.length; i2++) {
